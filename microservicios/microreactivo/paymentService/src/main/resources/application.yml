server:
  port: 8085

spring:
  application:
    name: payment-service

  r2dbc:
    url: r2dbc:postgresql://dbubik.postgres.database.azure.com:5432/postgres?ssl=true #${SPRING_R2DBC_URL}
    username: adminubik #${SPRING_R2DBC_USERNAME}
    password: juliannicolas@1 #${SPRING_R2DBC_PASSWORD}
    pool:
      initial-size: 3
      max-size: 10
      max-idle-time: 30m

  sql:
    init:
      mode: never

# ── Stripe ────────────────────────────────────────────────────────────────────
stripe:
  secret-key: sk_test_51Sx6ZoASBcriNpkYNLLTIxSEKessALDpW7L4mc1Yq7QDowoClCUt7UiBjOIdo0p4AyZ6AY3aQtAYjAlnVxMkoDfA00QMjyyEIf #${STRIPE_SECRET_KEY}
  publishable-key: pk_test_51Sx6ZoASBcriNpkYgq7h7WzJgsp6KIjkHIwrZHws5DuUyjShj43y1KTG43e4AEWKAIQ6BDFalWUGCnNIDKCTpbvJ00HmH9Utrk #${STRIPE_PUBLISHABLE_KEY}
  webhook-secret: whsec_c3aad5da824cc1dc2b4e4acbbc16f91ec3c81983c45cf10775b51e53fd9deb44 #${STRIPE_WEBHOOK_SECRET}

# ── URLs inter-servicio ───────────────────────────────────────────────────────
services:
  motel-management:
    url: ${MOTEL_MANAGEMENT_URL:http://localhost:8083}
  user-management:
    url: ${USER_MANAGEMENT_URL:http://localhost:8081}
  notification-service:
    url: ${NOTIFICATION_SERVICE_URL:http://localhost:8084}

# Logging
logging:
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - traceId=%X{traceId} spanId=%X{spanId} - %msg%n"
  level:
    root: INFO
    com.ubik.paymentservice: DEBUG
    com.example: DEBUG

# CONFIGURACIÓN DE OBSERVABILIDAD (Actuator + Micrometer)
management:
  endpoints:
    web:
      exposure:
        include: "health,info,prometheus,metrics"
      base-path: /actuator
  endpoint:
    health:
      show-details: always
      probes:
        enabled: true   # Habilita /actuator/health/liveness y /readiness
    prometheus:
      enabled: true
  metrics:
    export:
      prometheus:
        enabled: true
    tags:
      service: payment
      environment: ${SPRING_PROFILES_ACTIVE:local}
    distribution:
      # Histogramas para percentiles de latencia HTTP
      percentiles-histogram:
        http.server.requests: true
      percentiles:
        http.server.requests: 0.5, 0.75, 0.95, 0.99
      slo:
        http.server.requests: 100ms, 500ms, 1s, 2s